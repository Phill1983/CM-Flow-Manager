# Placeholder directories

These directories are reserved for Phase 1+ implementation:

- `apps/` — Electron desktop application
- `packages/` — shared libraries
- `modules/` — feature modules (pdf-password-remover first)
- `scripts/` — maintenance and fixture scripts
- `assets/` — icons and static packaging assets

Do not add runtime application code here during Phase 0.
